name: Update Readme.md with date
on:
  schedule:
  - cron: '*/5 * * * *'

jobs:
  write-date:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
        
      - name: write date
        run: echo "Date now is $(date)" 
